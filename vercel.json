{
  "buildCommand": "ng build --configuration=production",
  "outputDirectory": "dist/mkr-commerce-admin/browser",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
